#// vim: set ft=python:

def test_for_securityContext():
    if spec["kind"] == "Deployment":
        for container in spec["spec"]["template"]["spec"]["containers"]:
			print ('Image:'+container["image"])
			
			assert_contains(container, "securityContext", "should have securityContext")

test_for_securityContext()